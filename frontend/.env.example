# Frontend Environment Variables
# In unified deployment mode, leave NEXT_PUBLIC_API_URL empty

# Backend API URL (empty = same origin, for unified deployment)
NEXT_PUBLIC_API_URL=

# Only set this if deploying frontend separately:
# NEXT_PUBLIC_API_URL=https://api.your-domain.com
